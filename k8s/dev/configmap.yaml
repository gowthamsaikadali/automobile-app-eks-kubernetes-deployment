apiVersion: v1
kind: ConfigMap
metadata:
  name: autoforge-config
  namespace: dev
data:
  APP_NAME: "AutoForge Dashboard"
  APP_ENV: "dev"
  APP_VERSION: "1.0.0"
  DB_HOST: "autoforge-db.dev.svc.cluster.local"
  DB_NAME: "autoforge_dev"
  DB_USER: "autoforge_user"
  FEATURE_FLAG_DARK_MODE: "true"
