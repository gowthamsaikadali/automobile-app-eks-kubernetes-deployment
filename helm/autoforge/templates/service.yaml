apiVersion: v1
kind: Service
metadata:
  name: autoforge-clusterip
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: autoforge
  ports:
    - port: 80
      targetPort: 5000
